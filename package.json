{
  "name": "mcp-as-a-judge",
  "version": "1.0.0",
  "description": "AI-powered software engineering validation tools for MCP",
  "main": "cloudflare-worker/worker.py",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "cd cloudflare-worker && uv run pywrangler dev",
    "build": "cd cloudflare-worker && uv run pywrangler build"
  },
  "keywords": ["mcp", "ai", "validation", "cloudflare", "workers"],
  "author": "MCP as a Judge",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.33.1"
  }
}
