# MCP as a Judge Configuration
# Copy this file to .env and configure your settings

# Transport Configuration
# Options: "stdio" or "sse"
TRANSPORT=stdio

# Server Configuration (for SSE transport)
HOST=0.0.0.0
PORT=8050

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=json

# Development Settings
DEBUG=false
DEVELOPMENT_MODE=false

# LLM fallback configuration (optional)
# Set a custom model and base URL when using OpenAI-compatible endpoints.
# LLM_API_KEY=your-api-key-here
# LLM_MODEL_NAME=gpt-4.1
# LLM_BASE_URL=https://api.example.com/v1

# Health Check Configuration
HEALTH_CHECK_ENABLED=true
HEALTH_CHECK_INTERVAL=30

# Performance Settings
MAX_CONCURRENT_REQUESTS=10
REQUEST_TIMEOUT=30

# Security Settings
CORS_ENABLED=false
CORS_ORIGINS=*

# Example MCP Client Configuration
# Add this to your Claude Desktop or Cursor configuration:
#
# For SSE Transport:
# {
#   "mcpServers": {
#     "mcp-as-a-judge": {
#       "transport": "sse",
#       "url": "http://localhost:8050/sse"
#     }
#   }
# }
#
# For Stdio Transport:
# {
#   "mcpServers": {
#     "mcp-as-a-judge": {
#       "command": "uv",
#       "args": ["run", "mcp-as-a-judge"],
#       "env": {
#         "TRANSPORT": "stdio"
#       }
#     }
#   }
# }
